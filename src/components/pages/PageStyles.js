import styled from 'styled-components';
import { injectGlobal } from 'styled-components';

export const About = ``

export const Featured = ``

export const Hero = ``

export const Footer = ``

export const Info = ``

export const Home = ``

export const Team = ``